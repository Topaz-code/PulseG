{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the dashboard is allowed to ask the desktop shell to do. It is deliberately short: the webview renders the interface, the Python sidecar does the work, and the shell only opens windows the user asked for.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "core:event:default",
    "shell:allow-open",
    "opener:default",
    "dialog:allow-message",
    "dialog:allow-confirm",
    "notification:default"
  ]
}
